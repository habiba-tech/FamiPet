// Placeholder app shell — replaced by routed pages (migration.md §§2, 4).
// Uses global tokens (docs/design.md §1.1) so they stay exercised by JIT.
function App() {
  return (
    <div className="p-8 text-center text-base">
      <h1 className="mb-2 text-3xl font-extrabold tracking-tight text-primary">FamiPet</h1>
      <span className="inline-block rounded-full bg-primary-light px-4 py-1 text-sm font-medium text-primary-dark">
        React frontend — migration in progress
      </span>
    </div>
  )
}

export default App