import { Outlet } from 'react-router-dom'

// Area layout shells — STUBS for Phase 4 (routing). Replaced by the real
// layout components in Phase 6 (shared layout components).

export function LandingLayout() {
  return <Outlet />
}

export function AuthLayout() {
  return <Outlet />
}

export function AppLayout() {
  return <Outlet />
}

export function AdminLayout() {
  return <Outlet />
}